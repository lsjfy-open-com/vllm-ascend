# 实验关键证据回传

只填写来自允许回传文件的内容。未知填“未观察到”，不得补猜。保留采集器生成的限制说明，再追加下列分析。

## 运行范围

- 用户确认的测试基线：**0.25rc1**。声明不等于运行时验证；不得引用 0.23 的结果作为本轮验证。
- 本次角色/实例与 `correlation_scope`：
- P / D / proxy 是否同一次尝试：
- 实际 vLLM Ascend / vLLM 安装版本及各自 commit；是否 dirty；与 0.25rc1 基线是否相符：
- Python / torch-npu / Mooncake / CANN / HiXL 版本及各自来源：
- 是否在服务相同容器和 Python 环境执行；哪些尚未证实：
- coverage 截断、超长行、歧义、日志变动、样本预算缺失：

## 配置事实

| 项目 | P | D | 来源与证据 |
| --- | --- | --- | --- |
| TP / DP / PP / PCP / DCP | 未观察到 | 未观察到 | |
| Connector 类型及顺序 | 未观察到 | 未观察到 | |
| MTP / speculative token 数 | 未观察到 | 未观察到 | |
| shared-expert overlap | 未观察到 | 未观察到 | |
| fused / prefill / hierarchy MC2 | 未观察到 | 未观察到 | |
| FA / KV C8 / sparse SFA C8 / LI C8 | 未观察到 | 未观察到 | |
| block size / graph / KV offload | 未观察到 | 未观察到 | |

## 首个有效异常与阶段

- 首个非重复具体异常证据编号（例如 `P0:E001`）：
- 内层异常类型、受控原因、关键文件/函数/行号：
- 相同异常的 rank 分布和次数：
- 最后观察到的成功阶段；是否已有请求到达的证据：
- 后续 worker 退出/通信错误是否可能为连带现象：

## Proxy 与单请求链路

- 实际 proxy 入口、SHA256、layerwise 标记：
- 同一个匿名请求：D 目标块分配 → callback/派发 → P 映射/传输 → 完成信号 → D 完成。每步填证据编号或“未观察到”。
- 如果未重新实验，明确写“本轮未发请求”；不能把旧 proxy 结果写成本轮。

## 分析与下一步

| 观察事实 | 待验证假设 | 缺失证据 | 最小下一步 |
| --- | --- | --- | --- |
| | | | |

明确区分“代码审查中存在的问题”与“本次运行确实命中”。不要输出未经允许的原始行、请求文本、真实路径/IP、ptr 或凭证。没有 NPU 实测就写明，不能把本地合成测试当作实机测试。
